# Forge Runtime Templates

This package directory contains only target-repository runtime/template assets used by the `forge` CLI.

Engine-only docs, specs, validation cases, adapters, and skills are intentionally excluded from this packaged payload.
