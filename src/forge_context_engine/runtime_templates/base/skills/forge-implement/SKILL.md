# forge-implement

## Purpose
Legacy compatibility alias for `forge-implementation`.

## Load
Use `.forge/skills/forge-implementation/SKILL.md` and `.forge/context/modes/implementation.md`.

## Invocation
Accept only for older prompts that ask for `forge-implement`. Treat it as `forge-implementation`.

## Focus
Generate an ECP from an approved plan. Preserve Gate 2: human approval is required before `execute`.

## Output
Return implementation-mode ECP output.

## Do NOT
Do not edit code or treat this legacy alias as a separate core lifecycle mode.
