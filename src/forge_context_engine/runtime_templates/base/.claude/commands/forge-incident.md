# /forge-incident

Compatibility wrapper for incident scenario guidance.

Use shared skill:
`.forge/skills/forge-incident/SKILL.md`

Incident is not a core lifecycle mode. Route diagnosis/remediation through `ask`, `plan`, `implementation`, `execute`, and `review`.
